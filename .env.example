# Copia este archivo a .env en la raíz del proyecto y ajusta los valores.
# No comites el archivo .env con credenciales reales.

# URL base del backend
API_BASE=https://api.example.com

# (Opcional) API key de TMDB si la usas en el backend o frontend
TMDB_API_KEY=
